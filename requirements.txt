# TalonResponse - Python Dependencies
# Built by Rayyan Umair
# "Verify the threat. Execute the isolation. Preserve the evidence."
#
# Install: pip install -r requirements.txt
# Requires: Python 3.11+

# -- Web Framework ------------------------------------------------------------
fastapi==0.115.5
uvicorn[standard]==0.32.1

# -- Settings and Validation --------------------------------------------------
pydantic==2.10.3
pydantic-settings==2.6.1

# -- ZeroMQ Transport ---------------------------------------------------------
pyzmq==26.2.0

# -- Database -----------------------------------------------------------------
duckdb==1.1.3

# -- Data Processing ----------------------------------------------------------
pandas==2.2.3
pyarrow==18.1.0

# -- HTTP Client (ecosystem callbacks) ----------------------------------------
httpx==0.28.1

# -- YAML Playbook Parsing ----------------------------------------------------
PyYAML==6.0.2

# -- Utilities ----------------------------------------------------------------
python-dotenv==1.0.1
python-multipart==0.0.20
websockets==14.1